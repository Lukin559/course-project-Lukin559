# Hadolint configuration for Dockerfile linting
# Hadolint checks Dockerfiles for best practices and common mistakes
# https://github.com/hadolint/hadolint

# Rules to ignore (with justification):
ignored:
  # DL3008: Pin versions in apt get install - not always practical for slim images
  # We control base image versions explicitly instead
  - DL3008
  
  # DL3018: Pin versions in apk add - not always practical
  # We control base image versions explicitly instead
  - DL3018

# Strict rules we want to enforce:
# - DL3000: Use absolute WORKDIR
# - DL3001: Avoid sudo in Dockerfile (use USER instead)
# - DL3002: Do not use root user
# - DL3003: Use WORKDIR for cd
# - DL4006: Use SHELL to set shell options
# - DL3007: Avoid using :latest tag
# - DL3025: Use JSON notation for CMD and ENTRYPOINT

# Trusted registries (optional - restrict base images)
# trustedRegistries:
#   - docker.io
#   - gcr.io

